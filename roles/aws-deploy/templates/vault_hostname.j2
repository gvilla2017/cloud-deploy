---
{% for host in servers_to_monitor %}
{% if hostvars[host].tags.group == 'webserver' %}
vault_hostname: {{ hostvars[host].ansible_host }}
{% endif %}
{% endfor %}
{% if entry == host and 'ansible_host' in hostvars[host] %}
